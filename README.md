Source files for TheSteelCo Website

To work on the website create a linux environment:
install pip

sudo apt-get install pip

use pip to install the requirements file:

sudo pip install -r requirements.txt